import { css } from '@emotion/react';

export const inputPwConfirmContainer = css({
  display: 'flex',
  justifyContent: 'space-between',
  width: '100%',
});

export const labelStyling = css({
  marginTop: '20px',
});
