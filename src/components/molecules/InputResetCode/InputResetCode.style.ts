import { css } from '@emotion/react';

export const inputResetCodeContainer = css({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  width: '100%',
});

export const inputResetCodeBtnStyling = css({
  padding: '10px 25px',
});
