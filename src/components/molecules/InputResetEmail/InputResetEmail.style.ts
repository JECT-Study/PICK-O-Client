import { css } from '@emotion/react';

export const inputResetEmailContainer = css({
  display: 'flex',
  justifyContent: 'space-between',
  width: '100%',
});

export const inputResetEmailBtnStyling = css({
  padding: '10px 25px',
});

export const labelStyling = css({
  marginTop: '20px',
});
